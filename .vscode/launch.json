{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Wails3: Debug Go Backend",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "cwd": "${workspaceFolder}"
        }
    ]
}